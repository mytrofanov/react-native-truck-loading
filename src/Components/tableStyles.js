import {StyleSheet} from "react-native";

export const tableStyles = StyleSheet.create({
    tableText: {
        flex:1,
        marginTop:'4%',
        color:'white',
        fontSize: 15
    },
});
